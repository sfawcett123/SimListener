namespace SimListener
{
    internal enum DEFINITION
    {
        Dummy = 0,
        MAX_DEFINITIONS = 100
    };
    internal enum REQUEST
    {
        Dummy = 0,
        ResultStructure,
        MAX_REQUESTS = 100
    };
    internal enum Event
    {
        RECUR_1SEC,
    }
    internal enum Requests
    {
        SIMULATION,
        AIRCRAFT_LOADED,
    }
}
